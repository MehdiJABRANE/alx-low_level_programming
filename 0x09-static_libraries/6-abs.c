#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - gives the abs of an integer
 *
 * @n: the number that will be operated
 *
 * Return: abs of an integer
*/

int _abs(int n)
{
	return (abs(n));
}
